<?php

namespace {repositoryNamespace};

use {modelNamespace}\{modelClass};
use {contractNamespace}\{repositoryClass} as {repositoryClass}Contract;

class {repositoryClass} extends {baseRepositoryClass} implements {repositoryClass}Contract
{
    /**
     * {repositoryClass} constructor.
     *
     * @param {modelClass} ${model}
     */
    public function __construct({modelClass} ${model})
    {
        $this->model = ${model};
    }
}